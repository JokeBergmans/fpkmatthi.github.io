# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
title: Matthias Van De Velde
email: skymatth@gmail.com
description: >- # this means to ignore newlines until "baseurl:"
  
baseurl: "/" # the subpath of your site, e.g. /blog
url: "https://fpkmatthi.github.io/" # the base hostname & protocol for your site, e.g. http://example.com
twitter_username: fpkmatthi
github_username:  fpkmatthi

# Build settings
markdown: kramdown
paginate_multiple:
  - paginate: 5
    paginate_path: '/posts/page:num/'
    sub_dir: '/en'
  - paginate: 5
    paginate_path: '/nl/posts/page:num/'
    sub_dir: '/nl'

plugins:
  - jekyll-feed
  - jekyll-paginate
  - jekyll-paginate-multiple

# Exclude from processing.
# The following items will not be processed, by default. Create a custom list
# to override the default setting.
exclude:
  - vendor
  - Gemfile
  - Gemfile.lock
  - LICENSE
  - README.md
  - Rakefile
